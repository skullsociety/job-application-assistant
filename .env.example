# Copy these settings into a private .env file only after API Gateway is created.
# Never commit the real token.
JOB_ASSISTANT_AWS_INGEST_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com/ingest
JOB_ASSISTANT_AWS_INGEST_TOKEN=replace-with-a-long-random-secret
