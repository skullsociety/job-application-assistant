# JobStreet adapter

This folder contains the JobStreet local service, capture parser and the adapter that serves the shared dashboard. Its browser module now lives in `extension/sites/jobstreet` and is loaded through the unified extension documented in [the main guide](../README.md).

Load only the root `extension/` folder in Chrome. Job application review and submission remain manual.
