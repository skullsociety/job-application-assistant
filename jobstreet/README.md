# JobStreet adapter

This folder contains the JobStreet-specific Chrome extension, capture parser and the adapter that serves the shared dashboard. It uses the suite-wide data and autofill components documented in [the main guide](../README.md).

Load `jobstreet/extension` as an unpacked Chrome extension after running the suite setup. Job application review and submission remain manual.
